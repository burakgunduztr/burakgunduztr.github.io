/**
 * config.js
 *
 */

const CONFIG = {

  /**
   * URL of the JSON file containing document data.
   * Use a local path for a file in this repo, or a full URL for another domain.
   * Remote URLs require the server to send: Access-Control-Allow-Origin configured.
   *
   * Examples:
   *   "data.json"
   *   "https://yourwebsite.com/assets/data.json"
   */
  dataUrl: "https://burakgunduz.dev/githubio/data.json",

  /**
   * Milliseconds of stagger delay added between each card's entrance animation.
   */
  cardStaggerMs: 40,

};
