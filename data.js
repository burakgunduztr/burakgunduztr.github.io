/**
 * data.js — reference copy, not loaded by the site.
 *
 * The site reads data.json (see CONFIG.dataUrl in config.js).
 * Edit data.json directly; this file is kept only as a human-readable
 * reference for the expected JSON structure:
 *
 * [
 *   {
 *     "category": "…",   — Group heading; entries with the same value are grouped.
 *     "title":    "…",   — Display name of the entry.
 *     "description": "…",— One-sentence summary shown beneath the title.
 *     "url":      "…",   — Website page or direct file link (PDF, DOCX, PPTX, …).
 *                          The card icon is set automatically by the URL extension.
 *     "updated":  "YYYY-MM-DD" — Used for the "last updated" label and newest-first sort.
 *   }
 * ]
 */
